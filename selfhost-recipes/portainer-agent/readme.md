# Portainer Agent (Edge)

## Installation
1. Generate Edge agent credentials in Portainer.
2. Fill `.env` (`EDGE`, `EDGE_ID`, `EDGE_KEY`, `EDGE_INSECURE_POLL`).
3. Deploy stack on the target host.
4. Confirm host appears as connected in Portainer.
