# Kutt

## Installation
1. Fill `.env` with domain, JWT secret, DB password, and SMTP values.
2. Create host folders for Kutt and MariaDB under `/var/docker/kutt/volumes`.
3. Deploy via Portainer.
4. Verify HTTPS access and mail delivery for auth flows.
