# Drone CI

## Installation
1. Fill `.env` with GitHub OAuth + RPC settings.
2. Create `/var/docker/drone/volumes/data`.
3. Deploy stack in Portainer.
4. Open `https://<TRAEFIK_HOST>` and complete initial authorization.
