# Vaultwarden

## Installation
1. Fill `.env` (`TRAEFIK_HOST`, `ADMIN_TOKEN`, SMTP settings).
2. Create `/var/docker/vaultwarden/volumes/data`.
3. Deploy stack via Portainer.
4. Validate admin panel access and SMTP configuration.
