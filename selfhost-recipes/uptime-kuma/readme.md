# Uptime Kuma

## Installation
1. Fill `.env` (`TRAEFIK_HOST`).
2. Create `/var/docker/uptime-kuma/volumes/data`.
3. Deploy stack via Portainer.
4. Complete first-user setup at `https://<TRAEFIK_HOST>`.
