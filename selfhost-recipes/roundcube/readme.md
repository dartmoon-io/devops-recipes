# Roundcube

## Installation
1. Fill `.env` with IMAP/SMTP endpoint values.
2. Create host folders under `/var/docker/roundcube/volumes`.
3. Deploy stack via Portainer.
4. Verify login and outbound email from webmail.
